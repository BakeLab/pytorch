diff --git a/cmake/BuildFlags.cmake b/cmake/BuildFlags.cmake
--- a/cmake/BuildFlags.cmake
+++ b/cmake/BuildFlags.cmake
@@ -264,7 +264,8 @@ function(torch_xpu_ops_finalize_targets)
     target_compile_options(${lib} PRIVATE ${TORCH_XPU_OPS_FLAGS})

     target_include_directories(${lib} PUBLIC
-        ${TORCH_XPU_OPS_INCLUDE_DIRS} ${ATen_XPU_INCLUDE_DIRS} ${SYCL_INCLUDE_DIR})
+        ${TORCH_XPU_OPS_INCLUDE_DIRS} ${ATen_XPU_INCLUDE_DIRS})
+    target_include_directories(${lib} SYSTEM PUBLIC ${SYCL_INCLUDE_DIR})

     target_link_libraries(${lib}
         PUBLIC  ${SYCL_LIBRARY} ${ARGN}
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -37,6 +37,6 @@ endif()

 if(USE_ONEMKL_XPU)
   target_compile_definitions(torch_xpu_ops PRIVATE USE_ONEMKL_XPU)
-  target_include_directories(torch_xpu_ops PUBLIC ${TORCH_XPU_OPS_ONEMKL_INCLUDE_DIR})
+  target_include_directories(torch_xpu_ops SYSTEM PUBLIC ${TORCH_XPU_OPS_ONEMKL_INCLUDE_DIR})
   target_link_libraries(torch_xpu_ops PUBLIC ${TORCH_XPU_OPS_ONEMKL_LIBRARIES})
 endif()
