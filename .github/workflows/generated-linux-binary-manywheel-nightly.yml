# Owner(s): ["oncall: releng", "module: rocm", "module: xpu"]
# @generated DO NOT EDIT MANUALLY

# Template is at:    .github/templates/linux_binary_build_workflow.yml.j2
# Generation script: .github/scripts/generate_ci_workflows.py
name: linux-binary-manywheel

on:
  push:
    # NOTE: Meta Employees can trigger new nightlies using: https://fburl.com/trigger_pytorch_nightly_build
    branches:
      - nightly
    tags:
      # NOTE: Binary build pipelines should only get triggered on release candidate builds
      # Release candidate tags look like: v1.11.0-rc1
      - v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+
      - 'ciflow/binaries/*'
      - 'ciflow/binaries_libtorch/*'
      - 'ciflow/binaries_wheel/*'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  ALPINE_IMAGE: "308535385114.dkr.ecr.us-east-1.amazonaws.com/tool/alpine"
  AWS_DEFAULT_REGION: us-east-1
  BINARY_ENV_FILE: /tmp/env
  BUILD_ENVIRONMENT: linux-binary-manywheel
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_NUMBER: ${{ github.event.pull_request.number }}
  PYTORCH_FINAL_PACKAGE_DIR: /artifacts
  PYTORCH_ROOT: /pytorch
  SHA1: ${{ github.event.pull_request.head.sha || github.sha }}
  SKIP_ALL_TESTS: 0
concurrency:
  group: linux-binary-manywheel-${{ github.event.pull_request.number || github.ref_name }}-${{ github.ref_type == 'branch' && github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

jobs:
  get-label-type:
    if: github.repository == vars.BINARY_RELEASE_REPOSITORY
    name: get-label-type
    uses: pytorch/pytorch/.github/workflows/_runner-determinator.yml@main
    with:
      triggering_actor: ${{ github.triggering_actor }}
      issue_owner: ${{ github.event.pull_request.user.login || github.event.issue.user.login }}
      curr_branch: ${{ github.head_ref || github.ref_name }}
      curr_ref_type: ${{ github.ref_type }}
      check_experiments: lf

  # Computes the manylinux builder image tag suffix + registry host at runtime,
  # feeding the job-level `container:` directives (resolved before any step runs):
  #   - release-candidate tag (v1.2.3-rc4): suffix = "-<.ci/docker tree hash>",
  #     host = "" -> the reproducible image binary-docker-build pinned on Docker
  #     Hub.
  #   - ciflow/binaries PR test: the PR builds its own .ci/docker and
  #     binary-docker-build pushes the content-addressed image to ECR, so
  #     suffix = "-<hash>" and host = the ECR host. Because build-manywheel-images
  #     runs in parallel on the same PR, this job WAITS (polls ECR) until those
  #     images are pushed before the build/test jobs start pulling them.
  #   - otherwise (nightly branch, etc.): skipped; outputs read blank -> Docker
  #     Hub floating tag, unchanged.
  get-docker-tag:
    # Runs on tag pushes only (release candidates + ciflow/binaries). On branch
    # (nightly) runs it is skipped and its outputs read blank, so the floating
    # Docker Hub tag is used.
    if: ${{ github.repository == vars.BINARY_RELEASE_REPOSITORY && github.ref_type == 'tag' }}
    name: get-docker-tag
    runs-on: ubuntu-24.04
    permissions:
      id-token: write
      contents: read
    outputs:
      docker_image_suffix: ${{ steps.calc.outputs.docker_image_suffix }}
      docker_registry_host: ${{ steps.calc.outputs.docker_registry_host }}
    steps:
      - name: Checkout PyTorch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          submodules: false
          show-progress: false
      - name: Calculate docker image suffix and registry host
        id: calc
        env:
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
          GITHUB_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail
          suffix=""
          registry_host=""
          if [[ "${GITHUB_REF}" == refs/tags/ciflow/binaries* ]]; then
            # PR test run: the content-addressed image lives in ECR.
            suffix="-$(git rev-parse HEAD:.ci/docker)"
            registry_host="308535385114.dkr.ecr.us-east-1.amazonaws.com/"
          elif [[ "${REF_TYPE}" == "tag" && "${REF_NAME}" == v* ]]; then
            # Release candidate: pin the Docker Hub image.
            suffix="-$(git rev-parse HEAD:.ci/docker)"
          fi
          echo "docker_image_suffix=${suffix}" >> "${GITHUB_OUTPUT}"
          echo "docker_registry_host=${registry_host}" >> "${GITHUB_OUTPUT}"
      # ciflow/binaries only: the builder images are pushed to ECR by
      # build-manywheel-images / build-almalinux-images, which run concurrently
      # on the same PR. Log in (read-only) and wait for the content-addressed
      # tags to appear before the build/test jobs try to pull them.
      - name: Login to ECR
        if: ${{ startsWith(github.ref, 'refs/tags/ciflow/binaries') }}
        uses: pytorch/pytorch/.github/actions/ecr-login@main
      - name: Wait for content-addressed builder image in ECR
        if: ${{ startsWith(github.ref, 'refs/tags/ciflow/binaries') }}
        env:
          DOCKER_IMAGE_SUFFIX: ${{ steps.calc.outputs.docker_image_suffix }}
        shell: bash
        run: |
          set -euo pipefail
          # Drop the leading dash: "-<hash>" -> "<hash>".
          tree_hash="${DOCKER_IMAGE_SUFFIX#-}"
          # build-manywheel-images builds and pushes all variants together, so
          # wait on a single representative image as the readiness signal.
          repo="pytorch/manylinux2_28-builder"
          tag="cuda13.4-${tree_hash}"
          echo "Waiting for ${repo}:${tag} to be pushed to ECR by build-manywheel-images..."
          # Poll every 5 min, up to 60 min.
          deadline=$(( SECONDS + 60 * 60 ))
          until aws ecr describe-images \
              --region us-east-1 \
              --repository-name "${repo}" \
              --image-ids imageTag="${tag}" >/dev/null 2>&1; do
            if (( SECONDS > deadline )); then
              echo "ERROR: timed out (60 min) waiting for ${repo}:${tag} in ECR." >&2
              echo "Is build-manywheel-images / build-almalinux-images still running or did it fail?" >&2
              exit 1
            fi
            echo "  not in ECR yet (build-manywheel-images still running); retrying in 5 min..."
            sleep 300
          done
          echo "Found ${repo}:${tag}"

  manywheel-cuda-cu134-build:
    name: manywheel-cuda-cu134-build
    if: ${{ !failure() && !cancelled() && github.repository == vars.BINARY_RELEASE_REPOSITORY }}
    needs: [get-label-type, get-docker-tag]
    runs-on: "${{ needs.get-label-type.outputs.label-type }}${{ (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/nightly' || startsWith(github.ref, 'refs/heads/release/') || startsWith(github.ref, 'refs/tags/v')) && 'rel-l-x86iavx512-44-340' || 'l-x86iavx512-48-384' }}"
    timeout-minutes: 280
    container:
      image: ${{ needs.get-docker-tag.outputs.docker_registry_host }}pytorch/manylinux2_28-builder:cuda13.4${{ needs.get-docker-tag.outputs.docker_image_suffix }}
    env:
      PYTORCH_ROOT: ${{ github.workspace }}
      PACKAGE_TYPE: manywheel
      DESIRED_CUDA: cu134
      GPU_ARCH_VERSION: "13.4"
      GPU_ARCH_TYPE: cuda
      DOCKER_IMAGE: ${{ needs.get-docker-tag.outputs.docker_registry_host }}pytorch/manylinux2_28-builder:cuda13.4${{ needs.get-docker-tag.outputs.docker_image_suffix }}
      SKIP_ALL_TESTS: 1
      DESIRED_PYTHONS: "3.14 3.15"
      BUILD_NAME_PREFIX: "manywheel-py"
      BUILD_NAME_SUFFIX: "-cuda13_4"
      PYTORCH_EXTRA_INSTALL_REQUIREMENTS: "cuda-toolkit[nvrtc,cudart,cupti,cufft,cusolver,cusparse,cublas,cufile,nvjitlink,nvtx]==13.4.0rc1; platform_system == 'Linux' | cuda-bindings>=13.0.3,<14; platform_system == 'Linux' and python_version < '3.15' | nvidia-cudnn-cu13==9.25.1.1; platform_system == 'Linux' | nvidia-cusparselt-cu13==0.8.1; platform_system == 'Linux' | nvidia-nccl-cu13==2.31.2; platform_system == 'Linux' | nvidia-nvshmem-cu13==3.4.5; platform_system == 'Linux'"
    steps:
      - name: Configure git safe directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Checkout PyTorch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          # Full fetch on tag pushes so the release tag is visible to
          # `git describe --tags --exact`; nightlies stay shallow for speed.
          fetch-depth: ${{ github.ref_type != 'tag' && 2 || 0 }}
          fetch-tags: ${{ github.ref_type == 'tag' }}
          submodules: recursive
          show-progress: false
      - name: Populate binary env
        shell: bash
        run: |
          export PYTORCH_FINAL_PACKAGE_DIR="${RUNNER_TEMP}/artifacts"
          mkdir -p "${PYTORCH_FINAL_PACKAGE_DIR}"
          echo "PYTORCH_FINAL_PACKAGE_DIR=${PYTORCH_FINAL_PACKAGE_DIR}" >> "${GITHUB_ENV}"
          bash .ci/pytorch/binary_populate_env.sh
      - name: Build all wheels
        shell: bash
        run: |
          # shellcheck disable=SC1090,SC1091
          source "${BINARY_ENV_FILE}"
          bash .ci/manywheel/build_all.sh
      # One artifact per built Python -- names must match what the test/upload
      # matrix below downloads.
      - uses: actions/upload-artifact@v4.4.0
        with:
          name: manywheel-py3_14-cuda13_4
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/manywheel-py3_14-cuda13_4/*
      - uses: actions/upload-artifact@v4.4.0
        with:
          name: manywheel-py3_15-cuda13_4
          if-no-files-found: error
          path: ${{ runner.temp }}/artifacts/manywheel-py3_15-cuda13_4/*

  manywheel-build:
    name: ${{ matrix.build_name }}-build
    if: ${{ !failure() && !cancelled() && github.repository == vars.BINARY_RELEASE_REPOSITORY }}
    uses: ./.github/workflows/_binary-build-linux.yml
    needs: [get-label-type, get-docker-tag]
    strategy:
      fail-fast: false
      matrix:
        include:
          - desired_python: "3.14"
            desired_cuda: "rocm7.14"
            gpu_arch_version: "7.14"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm7.14"
            build_name: "manywheel-py3_14-rocm7_14-mi300x"
            timeout_minutes: 420
            pytorch_extra_install_requirements: "rocm[libraries,device-all]==7.14.*"
          - desired_python: "3.14"
            desired_cuda: "rocm10.0"
            gpu_arch_version: "10.0"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm10.0"
            build_name: "manywheel-py3_14-rocm10_0-mi300x"
            timeout_minutes: 420
            pytorch_extra_install_requirements: "rocm[libraries,device-all]==10.0.*"
          - desired_python: "3.14"
            desired_cuda: "xpu"
            gpu_arch_version: ""
            gpu_arch_type: "xpu"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "xpu"
            build_name: "manywheel-py3_14-xpu-bmg"
            timeout_minutes: 280
            pytorch_extra_install_requirements: ""
          - desired_python: "3.15"
            desired_cuda: "rocm7.14"
            gpu_arch_version: "7.14"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm7.14"
            build_name: "manywheel-py3_15-rocm7_14-mi300x"
            timeout_minutes: 420
            pytorch_extra_install_requirements: "rocm[libraries,device-all]==7.14.*"
          - desired_python: "3.15"
            desired_cuda: "rocm10.0"
            gpu_arch_version: "10.0"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm10.0"
            build_name: "manywheel-py3_15-rocm10_0-mi300x"
            timeout_minutes: 420
            pytorch_extra_install_requirements: "rocm[libraries,device-all]==10.0.*"
          - desired_python: "3.15"
            desired_cuda: "xpu"
            gpu_arch_version: ""
            gpu_arch_type: "xpu"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "xpu"
            build_name: "manywheel-py3_15-xpu-bmg"
            timeout_minutes: 280
            pytorch_extra_install_requirements: ""
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      DESIRED_CUDA: ${{ matrix.desired_cuda }}
      GPU_ARCH_VERSION: ${{ matrix.gpu_arch_version }}
      GPU_ARCH_TYPE: ${{ matrix.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.docker_image }}
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.docker_image_tag_prefix }}${{ needs.get-docker-tag.outputs.docker_image_suffix }}
      DOCKER_IMAGE_REGISTRY_HOST: "${{ needs.get-docker-tag.outputs.docker_registry_host }}"
      DESIRED_PYTHON: ${{ matrix.desired_python }}
      runner_prefix: "${{ needs.get-label-type.outputs.label-type }}"
      runs_on: ${{ (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/nightly' || startsWith(github.ref, 'refs/heads/release/') || startsWith(github.ref, 'refs/tags/v')) && 'rel-l-x86iavx512-44-340' || 'l-x86iavx512-48-384' }}
      use_container_directive: true
      ALPINE_IMAGE: "308535385114.dkr.ecr.us-east-1.amazonaws.com/tool/alpine"
      PYTORCH_EXTRA_INSTALL_REQUIREMENTS: ${{ matrix.pytorch_extra_install_requirements }}
      timeout-minutes: ${{ matrix.timeout_minutes }}
      build_name: ${{ matrix.build_name }}
      build_environment: linux-binary-manywheel
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  manywheel-cuda-cu134-test:
    name: ${{ matrix.build_name }}-test
    if: ${{ !failure() && !cancelled() && github.repository == vars.BINARY_RELEASE_REPOSITORY }}
    needs: [get-label-type, get-docker-tag, manywheel-cuda-cu134-build]
    uses: ./.github/workflows/_binary-test-linux.yml
    strategy:
      fail-fast: false
      matrix:
        include:
          - desired_python: "3.14"
            desired_cuda: "cu134"
            gpu_arch_version: "13.4"
            gpu_arch_type: "cuda"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "cuda13.4"
            build_name: "manywheel-py3_14-cuda13_4"
            runs_on: "l-x86iavx512-29-115-t4"
          - desired_python: "3.15"
            desired_cuda: "cu134"
            gpu_arch_version: "13.4"
            gpu_arch_type: "cuda"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "cuda13.4"
            build_name: "manywheel-py3_15-cuda13_4"
            runs_on: "l-x86iavx512-29-115-t4"
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      DESIRED_CUDA: ${{ matrix.desired_cuda }}
      GPU_ARCH_VERSION: ${{ matrix.gpu_arch_version }}
      GPU_ARCH_TYPE: ${{ matrix.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.docker_image }}
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.docker_image_tag_prefix }}${{ needs.get-docker-tag.outputs.docker_image_suffix }}
      DOCKER_IMAGE_REGISTRY_HOST: "${{ needs.get-docker-tag.outputs.docker_registry_host }}"
      DESIRED_PYTHON: ${{ matrix.desired_python }}
      build_name: ${{ matrix.build_name }}
      build_environment: linux-binary-manywheel
      runner_prefix: "${{ needs.get-label-type.outputs.label-type }}"
      runs_on: ${{ matrix.runs_on }}
      use_container_directive: true
      ALPINE_IMAGE: "308535385114.dkr.ecr.us-east-1.amazonaws.com/tool/alpine"
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  manywheel-test-rocm:
    name: ${{ matrix.build_name }}-test
    if: ${{ !failure() && !cancelled() && github.repository == vars.BINARY_RELEASE_REPOSITORY }}
    needs: [get-label-type, get-docker-tag, manywheel-build]
    uses: ./.github/workflows/_binary-test-rocm-linux.yml
    strategy:
      fail-fast: false
      matrix:
        include:
          - desired_python: "3.14"
            desired_cuda: "rocm7.14"
            gpu_arch_version: "7.14"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm7.14"
            build_name: "manywheel-py3_14-rocm7_14-mi300x"
          - desired_python: "3.14"
            desired_cuda: "rocm10.0"
            gpu_arch_version: "10.0"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm10.0"
            build_name: "manywheel-py3_14-rocm10_0-mi300x"
          - desired_python: "3.15"
            desired_cuda: "rocm7.14"
            gpu_arch_version: "7.14"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm7.14"
            build_name: "manywheel-py3_15-rocm7_14-mi300x"
          - desired_python: "3.15"
            desired_cuda: "rocm10.0"
            gpu_arch_version: "10.0"
            gpu_arch_type: "rocm"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "rocm10.0"
            build_name: "manywheel-py3_15-rocm10_0-mi300x"
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      DESIRED_CUDA: ${{ matrix.desired_cuda }}
      GPU_ARCH_VERSION: ${{ matrix.gpu_arch_version }}
      GPU_ARCH_TYPE: ${{ matrix.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.docker_image }}
      # ROCm test runners have no ECR pull credentials, so ROCm stays on the
      # Docker Hub image (excluded from the content-addressed ECR path).
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.docker_image_tag_prefix }}
      DESIRED_PYTHON: ${{ matrix.desired_python }}
      build_name: ${{ matrix.build_name }}

  manywheel-test-xpu:
    name: ${{ matrix.build_name }}-test
    if: ${{ !failure() && !cancelled() && github.repository == vars.BINARY_RELEASE_REPOSITORY }}
    needs: [get-label-type, get-docker-tag, manywheel-build]
    uses: ./.github/workflows/_binary-test-xpu-linux.yml
    strategy:
      fail-fast: false
      matrix:
        include:
          - desired_python: "3.14"
            desired_cuda: "xpu"
            gpu_arch_type: "xpu"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "xpu"
            build_name: "manywheel-py3_14-xpu-bmg"
          - desired_python: "3.15"
            desired_cuda: "xpu"
            gpu_arch_type: "xpu"
            docker_image: "manylinux2_28-builder"
            docker_image_tag_prefix: "xpu"
            build_name: "manywheel-py3_15-xpu-bmg"
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      DESIRED_CUDA: ${{ matrix.desired_cuda }}
      GPU_ARCH_TYPE: ${{ matrix.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.docker_image }}
      # XPU test runners have no ECR pull credentials, so XPU stays on the
      # Docker Hub image (excluded from the content-addressed ECR path).
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.docker_image_tag_prefix }}
      DESIRED_PYTHON: ${{ matrix.desired_python }}
      build_name: ${{ matrix.build_name }}
